# copy to .env; the proxy is the Privatemode proxy (privatemode-proxy container), the key is passed through per request
SYSTEM_ONE_BASE_URL=http://proxy-host:8082/v1
SYSTEM_ONE_API_KEY=your-privatemode-api-key
